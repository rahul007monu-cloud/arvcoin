{
  "id": "FEAT-005",
  "type": "feat",
  "description": "PART 2d: make BTC, ETH, SOL and XRP clickable assets whose charts render tick-by-tick like ARV/BTC. Today ingest_prices() hardcodes only ETH+SOL into asset_candles and market.php only serves ARV candles (arv_candles) plus a watchlist snapshot; there is NO per-asset candle endpoint and XRP is absent. Add XRP, make BTC chartable as an asset, add per-asset candle backfill+rollup, expose a per-asset candle API, and add a frontend asset-selection UI that reuses the same live-feed tick-by-tick chart wiring from FEAT-003. This is the largest feature and depends on FEAT-003 (live-candle chart plumbing). Independent of FEAT-002/004 except that it reuses their patterns.",
  "steps": [
    "Add XRP to the config. In arv-config.js WATCHLIST add an XRP entry ({ key:'XRP', name:'XRP', colour:'#...', symbols:{ binance:'XRPUSDT', okx:'XRP-USDT', coinbase:'XRP-USD', kraken:'XRPUSD', coingecko:'ripple' } }). Verify each per-source symbol is valid (Kraken uses XRPUSD; Coinbase XRP-USD; OKX XRP-USDT; Binance XRPUSDT). Also add ETH/SOL kraken ws symbols and XRP to js/feed.js kraken.wsSymbols map ({ XBTUSD:'BTC/USD', ETHUSD:'ETH/USD', SOLUSD:'SOL/USD', XRPUSD:'XRP/USD' }).",
    "Generalise ingestion in api/_feed.php: replace the hardcoded foreach(['ETH','SOL']) in ingest_prices() with a list derived from the config watchlist assets (ETH, SOL, XRP) - keep BTC ingested by its dedicated path (it already writes asset_candles for BTC). So asset_candles ends up holding BTC, ETH, SOL, XRP 1m candles.",
    "Add per-asset backfill + rollup so the non-ARV assets have chartable history and larger timeframes, mirroring the ARV logic: add a backfill_asset_tf(assetKey, tf, days) (or extend backfill_tf to take an asset target writing to asset_candles instead of arv_candles) using fetch_candles_range(assetKey, tf, from); add a rollup for asset_candles larger tfs (5m/15m/1h/4h/1D/1W) analogous to rollup_all() but per asset_key writing to asset_candles. Wire these into the auto-backfill/cron path OR into an operator backfill action; keep it paced (usleep) like the existing paths. Note the deep-history honesty limit from FEAT-004 applies to these assets too (daily/weekly deep, minute recent).",
    "Expose a per-asset candle API. In api/market.php add an action (e.g. action=asset_candles&key=ETH&tf=1m&days=..&limit=..) that validates key against an allowlist (BTC,ETH,SOL,XRP), validates tf against the timeframe list, and reads asset_candles WHERE asset_key=? AND tf=? (mirroring handle_candles which reads arv_candles). Return the same {tf,count,from,to,candles:[{t,o,h,l,c,v}]} shape. Also extend handle_watchlist() to include XRP (add 'XRP'=>'XRP' to the loop map).",
    "Add a JS API function in js/api.js: assetCandles(key, tf, days, limit) that calls request('market','asset_candles',{method:'GET',query:{key,tf,days,limit}}), alongside the existing candles().",
    "Add the asset-selection UI + chart. On the page where charts live (trade.html and/or dashboard/portfolio - choose trade.html as the primary trading chart, and/or a market page), add clickable asset tabs/buttons for ARV, BTC, ETH, SOL, XRP. When ARV is selected use api.candles() (arv_candles) as today; when a coin is selected use api.assetCandles(key,...) and price/scale in USD (assets are quoted in USD in asset_candles). Reuse the SAME chart component and the SAME live tick-by-tick update wiring from FEAT-003, but for a coin update the last candle directly from feed.priceUsd(key) (no ARV conversion needed - the coin chart is in USD), and for ARV keep the BTC->ARV conversion. js/feed.js already subscribes to all BASKET+WATCHLIST assets (allAssets()), and now that XRP is in WATCHLIST the feed will stream XRP trades too - so feed.onTick/feed.priceUsd('XRP') will work. Confirm handleTrade only pushes BASKET (BTC) to the tape but updates state.prices for ALL assets (it does: state.prices[t.key] is set unconditionally), so per-asset live prices are available for every selected asset.",
    "Ensure each selected asset's chart updates tick-by-tick: on asset selection, load its historical candles then subscribe to feed ticks filtered to that asset key, updating the last bar in place (append new bar at tf-bucket boundary), identical to the ARV wiring. Switching assets should tear down the previous tick subscription cleanly (feed.onTick returns an unsubscribe function - use it).",
    "Do NOT let non-BTC assets affect ARV pricing or the money path: asset_candles for ETH/SOL/XRP are display-only (weight 0), exactly as the existing comments in _feed.php and market.php insist. The index still tracks BTC at weight 1.0."
  ],
  "acceptance_criteria": [
    "arv-config.js WATCHLIST includes XRP with valid per-source symbols, and js/feed.js kraken.wsSymbols includes XRP/USD (and ETH/SOL).",
    "api/_feed.php ingests ETH, SOL and XRP (derived from the watchlist, not a hardcoded ['ETH','SOL']) into asset_candles, and provides per-asset backfill + rollup writing to asset_candles for the chart timeframes.",
    "api/market.php serves a per-asset candle action (allowlisted key BTC/ETH/SOL/XRP, validated tf) returning the standard candle shape, and handle_watchlist() includes XRP.",
    "js/api.js exposes assetCandles(key,tf,days,limit).",
    "The frontend has clickable asset selection (ARV, BTC, ETH, SOL, XRP); selecting a coin loads its candles and updates its last candle tick-by-tick from the live js/feed.js stream (USD for coins, BTC->ARV conversion for ARV), reusing the FEAT-003 chart component; switching assets unsubscribes the previous tick listener.",
    "ETH/SOL/XRP remain weight 0 and never enter the index or the money path."
  ],
  "verification": [
    "`php -l api/_feed.php && php -l api/market.php` report no syntax errors.",
    "`node --check js/api.js && node --check js/feed.js && node --check js/pages/trade.js` (and any other edited page) report no parse errors.",
    "`node -e \"globalThis.window={};require('./arv-config.js');var w=globalThis.ARV_CONFIG.WATCHLIST.map(function(a){return a.key});console.log(w);if(w.indexOf('XRP')<0)throw'XRP missing'\"` prints the watchlist including XRP.",
    "`grep -n \"asset_candles\\|action=='asset_candles'\\|'asset_candles'\\|XRP\" api/market.php api/_feed.php` shows the per-asset endpoint, XRP in the watchlist loop, and the generalised ingest.",
    "`grep -n \"assetCandles\" js/api.js` and `grep -n \"assetCandles\\|feed.priceUsd\\|onTick\" js/pages/trade.js` show the wiring.",
    "MANUAL/DEFERRED (record in findings): per-asset ingest/backfill and live streaming cannot be exercised without live exchange access + MySQL; verify on a live environment that asset_candles fills for BTC/ETH/SOL/XRP and that each asset's chart streams tick-by-tick. State this limit clearly."
  ],
  "blocked_reason": null,
  "status": "completed",
  "findings": "IMPLEMENTED\n\nConfig (arv-config.js): added XRP to WATCHLIST with symbols { binance:'XRPUSDT', okx:'XRP-USDT', coinbase:'XRP-USD', kraken:'XRPUSD', coingecko:'ripple' }, colour #7f8593. Verified per-source symbols against the FEAT-005 spec (Kraken XRPUSD, Coinbase XRP-USD, OKX XRP-USDT, Binance XRPUSDT). Node check confirms WATCHLIST = [ETH, SOL, XRP].\n\njs/feed.js: added XRPUSD:'XRP/USD' to kraken.wsSymbols (ETH/SOL already present). allAssets()=BASKET+WATCHLIST already subscribes every asset and handleTrade() sets state.prices[t.key] unconditionally for ALL assets, so feed.priceUsd('XRP'/'ETH'/'SOL') works now that XRP is in WATCHLIST; only BASKET (BTC) is pushed to the tape, unchanged.\n\napi/_feed.php: replaced the hardcoded foreach(['ETH','SOL']) in ingest_prices() with foreach(watchlist_keys()) (new helper returns ['ETH','SOL','XRP']; asset_keys() = BTC+watchlist). BTC still ingested via its dedicated asset_candles path. Added backfill_asset_tf(assetKey,tf,days) writing USD candles to asset_candles (mirrors backfill_tf but no fx/index conversion, same tiering + launch clamp + honesty limit) and rollup_assets() (per-asset USD twin of rollup_all for 5m/15m/1h/4h/1D/1W + Monday-aligned 1W). ingest_prices() now also calls rollup_assets() each tick. Extended the auto-backfill chain: auto_backfill_step()/auto_backfill_after() now understand 'ETH:1D' style steps and run ARV -> ETH -> SOL -> XRP, each through 1D->1W->1h->15m->5m->1m, then done; paced by fetch_candles_range()'s existing usleep. Return keys renamed tf->step where a step can be an asset step.\n\napi/market.php: added action=asset_candles -> handle_asset_candles() validating key against allowlist [BTC,ETH,SOL,XRP] and tf against the timeframe list, reading asset_candles WHERE asset_key=? AND tf=?, returning the same {key,tf,count,from,to,candles:[{t,o,h,l,c,v}]} shape as handle_candles (USD, no fx). handle_watchlist() now includes XRP (inIndex/weight already computed as BTC-only, so XRP is weight 0).\n\njs/api.js: added assetCandles(key,tf,days,limit) -> request('market','asset_candles',{method:'GET',query:{key,tf,days,limit}}).\n\nFrontend (trade.html + js/pages/trade.js): added a [data-asset-tabs] tab row (ARV, BTC, ETH, SOL, XRP). buildAssetTabs() switches st.asset, tears down the prior feed.onTick subscription (st.unsubTick) before discarding the series, and reloads the chart. loadChart() branches: ARV -> api.candles() (INR, keeps the BTC->ARV conversion, the Rs.1 launch line and the 'your cost' line); a coin -> api.assetCandles() in USD with 2dp/minMove 0.01, no launch/cost lines. livePrice() returns liveArvPrice() for ARV and feed.priceUsd(st.asset) for a coin; onLiveTick() uses it with the IDENTICAL bucket/append/series.update() mechanism from FEAT-003. resyncLiveBar() (30s backstop) branches to assetCandles for coins. OHLC readout uses fmtAssetPrice ($ for coins, Rs. for ARV).\n\nMONEY-PATH INVARIANT: ETH/SOL/XRP write only to asset_candles, never arv_candles or index_price(); grep confirms no watchlist key touches the index. Weight stays BTC=1.0, others 0.0. Coin charts are display-only USD.\n\nVERIFICATION RUN (sandbox): php -l api/_feed.php, api/market.php, api/cron.php + all api/*.php -> no syntax errors. env -u NODE_OPTIONS node --check js/api.js js/feed.js js/pages/trade.js -> parse OK. node -e config check -> WATCHLIST includes XRP with valid symbols. greps confirm asset_candles endpoint, XRP in watchlist loop, generalised ingest, assetCandles in api.js and the trade.js wiring (st.asset, feed.priceUsd, onTick, buildAssetTabs).\n\nDEFERRED RUNTIME CHECKS (no MySQL, no mail, no assumed exchange/WebSocket access in sandbox; must be verified on a live deploy): (1) that ingest_prices() fills asset_candles 1m for BTC/ETH/SOL/XRP and rollup_assets() maintains their 5m/15m/1h/4h/1D/1W buckets; (2) that the auto-backfill chain walks ARV then ETH/SOL/XRP and backfill_asset_tf pages real USD history (deep daily/weekly, recent minute) without fabricating candles; (3) that action=asset_candles returns populated series for each coin; (4) that selecting BTC/ETH/SOL/XRP on the trade chart streams tick-by-tick from the live feed (WebSocket trades channel) exactly like ARV, and that switching assets cleanly unsubscribes the previous tick listener. These require live exchange APIs + a populated DB and cannot be exercised here.\n\nSTEERING: steer-62f5ac03 ('or pahle wale me B option done') refers to a prior/other task's option choice outside FEAT-005's scope and the current task context has no 'B option'; noted, no action applicable within this feature. steer-a086b8a2 ('chart should be tick-by-tick in real time according to BTC') restates the core FEAT-005 requirement and is exactly what was implemented: every selected asset (incl. ARV which is BTC->INR) updates its last candle tick-by-tick from the live BTC/coin feed using the FEAT-003 mechanism.\n\n=== REVIEW-FIX PASS (2026-09-04 review, backfill failure isolation) ===\nAddressed review issue 3 in api/_feed.php auto_backfill_step():\n\n(3) ONE COIN CAN STALL THE WHOLE ASSET BACKFILL CHAIN (likely) - FIXED. Previously auto_backfill_fails was a single SHARED counter across every step, and any step that THREW (e.g. an exchange refusing to page a given coin's weekly) accumulated toward a hard cap of 20, at which point auto_backfill_next was set to 'stalled' - halting ARV/ETH/SOL/XRP backfill entirely, so a later coin never got its history. Now the fail counter is SCOPED to the current step via a new setting 'auto_backfill_fail_step': when the failing step differs from the recorded one the count restarts at 1, so a permanently-unpageable coin/timeframe can no longer burn a budget shared with the other coins. And after 20 refusals of the SAME step the chain SKIPS past it (setting auto_backfill_next = auto_backfill_after($next) and clearing the counter/fail_step) instead of stalling, so ARV -> ETH -> SOL -> XRP keeps walking; only the final step skipping lands on 'done', never 'stalled'. On success the fail_step is also cleared. Legacy handling of a pre-existing 'stalled' value is left intact for backward compatibility (early-return in auto_backfill_step and the weekly re-queue check in _schema.php still recognise it). An operator can still hard-stop the whole chain with auto_backfill=false.\n\nRE-VERIFIED IN-SANDBOX: `php -l api/_feed.php` clean. The change is control-flow only (per-step counter + skip-instead-of-stall); no exchange/DB access is exercisable in-sandbox, so the actual paging behaviour remains a deferred live check (confirm on a live install that a coin whose weekly an exchange refuses is skipped after N attempts while the remaining coins still backfill)."
}
